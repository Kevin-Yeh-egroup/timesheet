---
description: 時間資產轉換系統的核心架構、資料模型與設計哲學
alwaysApply: true
---

# 時間資產轉換系統 — 核心規則

## 產品核心哲學

此工具幫助使用者「看見累積、理解配置、發現可能性」。
語氣必須是**溫和、支持、引導**，絕不使用以下措辭：
- ❌ 評分、扣分、太少、不夠、浪費、機會成本、低效
- ✅ 累積、配置、觀察、可以嘗試、逐步、持續

---

## 技術架構

- **框架**：Next.js 16 App Router + TypeScript + React 19
- **狀態**：Zustand + persist（localStorage key: `time-records-storage`）
- **UI**：Tailwind CSS 4 + shadcn/ui（new-york 風格）
- **圖表**：Recharts
- **路由**：`/`（總覽）、`/add`（新增）、`/report`（月報表）

---

## 核心資料型別（`lib/types.ts`）

```typescript
type Category = "工作" | "學習" | "副業" | "人際" | "休息"
type ConversionStatus = "尚未轉換" | "已開始嘗試" | "已有成果"
type Asset = "體力" | "軟實力" | "硬實力" | "存款增加" | "收入" | "工具/副業基礎"
```

**難度標籤**（`getDifficultyLabel`）：1–2 = 輕鬆、3 = 中等、4–5 = 較有挑戰

**資產累積點數**：無形資產 ×1 + 有形資產 ×2 + 有產出 ×1.5

所有計算函式一律放在 `lib/types.ts`，頁面與元件只消費結果。

---

## 六維能力系統

| 能力 | emoji | 主要來源 | softCap |
|------|-------|---------|---------|
| 調整時間 | ⏱️ | 工作 × 0.4 + 類別多元性 | 35 |
| 增加體力 | 💪 | 休息 + 運動 × 2.5 | 20 |
| 強化能力 | 🧱 | 工作困難投入值 × 0.5 + 有產出 × 5 | 50 |
| 增加技能 | 📚 | 學習困難 × 1.0 + 副業困難 × 0.7 | 30 |
| 運用人脈 | 🤝 | 人際時數 × 2 + 人際產出 × 8 | 20 |
| 增加知識 | 💡 | 學習時數 × 1.5 + 學習困難 × 0.8 | 25 |

能力分數公式（Sigmoid）：`score = raw / (softCap + raw) × 100`

---

## 時間 → 能力歸因（圖表用，無重複）

| 類別 | 調整時間 | 增加體力 | 強化能力 | 增加技能 | 運用人脈 | 增加知識 |
|------|---------|---------|---------|---------|---------|---------|
| 工作 | ×0.3 | — | ×0.7 | — | — | — |
| 休息 | — | ×1.0 | — | — | — | — |
| 學習 | — | — | — | ×0.5 | — | ×0.5 |
| 副業 | — | — | — | ×1.0 | — | — |
| 人際 | — | — | — | — | ×1.0 | — |
