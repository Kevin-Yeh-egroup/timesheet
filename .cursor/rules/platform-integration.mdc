---
description: 平台串接、登入分流與社工版個案情境規則
alwaysApply: true
---

# 平台串接彈性規則

此工具未來會放入既有平台，開發時需保留登入狀態、使用者版本與個案情境的串接彈性。

## 版本情境

- 一般民眾版：維持目前個人時間紀錄、總覽、時段、AI 整理、摘要、紀錄與報表流程。
- 社工版：用於協助服務對象紀錄，未來需連動個案歷程紀要，並能嵌入個案檔案頁。
- 社工版入口需先有個案清單，再進入特定個案的時間盤點工具。

## 登入與入口

- 未註冊使用者先看到說明頁；說明頁是進入工具前的獨立 landing page，不使用工具內的主要導覽。
- 說明頁 Hero CTA 點擊後才正式進入工具。
- 已註冊使用者可直接進入工具，但工具內需保留清楚的說明入口，不放在主要側邊/底部導覽列。
- 登入判斷與角色/模式分流集中在平台 context 或入口層，不要散落在各頁面。

## 串接邊界

- 目前 localStorage store 是單機資料來源；未來接平台時不可假設它是唯一資料來源。
- 新增功能時優先透過 adapter/context 注入 `authStatus`、`audienceMode`、`currentCaseId` 等資訊。
- 計算邏輯仍放在 `lib/types.ts`，平台串接只改資料來源與入口流程，不改核心計算語意。
